{
    email carlostrevisan@gmail.com
}

realestate.carlostrevisan.xyz {
    tls {
        # Explicitly use ZeroSSL ACME to bypass Let's Encrypt rate limits
        issuer acme {
            dir https://acme.zerossl.com/v2/DV90
        }
    }

    # Ensure Caddy can handle ACME challenges before reverse proxying
    handle_path /.well-known/acme-challenge/* {
        file_server
    }

    # Everything else to the frontend
    handle {
        reverse_proxy frontend:3000
    }
}
